# Standalone frontend dev — run: docker compose up -d
# Requires backend running separately on :8889
services:
  frontend:
    build: .
    ports:
      - "8888:3000"
    env_file: ../.env
    environment:
      - BACKEND_URL=http://host.docker.internal:8889
